Ping an IP address:

ping 8.8.8.8

Here, we are using the IP address directly, so DNS is not required.

Ping a hostname:

ping google.com

Here, we are using a hostname. The computer first needs to find the IP address of google.com using DNS. Then it sends the ping.

Remember

Ping IP address → DNS is not required
Ping hostname → DNS is usually required
