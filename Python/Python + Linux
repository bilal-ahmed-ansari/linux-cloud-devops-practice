Create nano disk_check.py

Program:
import subprocess

result = subprocess.run(
    ["df", "-h"],
    capture_output=True,
    text=True
)

print(result.stdout)

Run:
python3 disk_check.py

You'll see the same type of output as:
df -h
