Strings are text.

hostname = "linux-server-01"

You can manipulate them:

hostname = "linux-server-01"
print(hostname)
print(hostname.upper())
print(hostname.lower())

Output:

linux-server-01
LINUX-SERVER-01
linux-server-01
