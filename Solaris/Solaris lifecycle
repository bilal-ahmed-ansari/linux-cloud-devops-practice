Zone Lifecycle

A Zone usually goes through these stages:

Configured
      ↓
Installed
      ↓
Ready
      ↓
Running

Example:

Configured → Zone settings (name, IP, storage) are created.
Installed → Solaris files are installed in the Zone.
Ready → Zone is prepared to start.
Running → Users can log in and applications run.
