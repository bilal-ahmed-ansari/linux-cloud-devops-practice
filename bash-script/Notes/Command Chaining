You can execute commands based on success/failure.

a)&&

Run the second command only if the first succeeds.

mkdir /tmp/test && echo "Directory created"

b)||

Run the second command if the first fails.

mkdir /tmp/test || echo "Failed to create directory"

c)Both
mkdir /tmp/test && echo "Success" || echo "Failed"
