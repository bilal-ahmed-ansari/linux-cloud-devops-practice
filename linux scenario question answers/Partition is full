First check disk usage.

df -h

Find large directories.

du -sh /*

Find large files.

find / -type f -size +500M

Check log files.

Check log files.

du -sh /var/log

Delete or rotate unnecessary logs if appropriate.

Also clean package cache if needed.

Ubuntu

sudo apt clean
